import { view, Component } from '@storefront/core';

@view('gb-terminal-pager', require('./index.html'), require('./index.css'))
class TerminalPager extends Component { }

export default TerminalPager;
